import { FC } from "react";

const Header: FC = () => {
    return (
        <main className="top-bar">
            <img src="src/assets/img/globe.png" alt="globe icon" />
            <h1>My Travel Journal</h1>
        </main>
    );
};

export default Header;
